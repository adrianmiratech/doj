// Punto de entrada dummy en JS para plataformas de hosting (p. ej. Discloud)
// que exigen que MAIN apunte a un archivo .js para detectar el entorno de
// ejecución. El bot real está en TypeScript (index.ts) y se arranca con el
// comando de START (`npm run bot`, que corre `tsx bot/index.ts`) — este
// archivo no se ejecuta en la práctica, solo satisface la validación de MAIN.
